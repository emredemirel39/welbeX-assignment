{
  "name": "welbex",
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently \"vite\" \"npm run server \"",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "server": "json-server -w api/db.json --port 1050"
  },
  "dependencies": {
    "vue": "^3.2.38"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^3.0.3",
    "concurrently": "^7.4.0",
    "json-server": "^0.17.0",
    "sass": "^1.55.0",
    "vite": "^3.0.9"
  }
}
